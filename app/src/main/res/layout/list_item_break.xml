<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
       xmlns:tools="http://schemas.android.com/tools"
       tools:ignore="UseCompoundDrawables">

    <include layout="@layout/timespan_layout"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:paddingBottom="8dp"
        android:paddingLeft="@dimen/activity_horizontal_margin">

        <ImageView
            android:layout_width="@dimen/track_icon_size"
            android:layout_height="@dimen/track_icon_size"
            android:contentDescription="@null"
            android:src="@drawable/ic_local_cafe_white_24dp"
            android:tint="@color/dark_gray"/>

        <TextView
            android:id="@+id/list_item_break_label"
            android:layout_width="match_parent"
            android:layout_height="@dimen/track_icon_size"
            android:gravity="center_vertical"
            android:paddingLeft="@dimen/activity_horizontal_margin"
            android:textColor="@color/primary_text_90"
            tools:text="Coffe break!"/>

    </LinearLayout>
</merge>