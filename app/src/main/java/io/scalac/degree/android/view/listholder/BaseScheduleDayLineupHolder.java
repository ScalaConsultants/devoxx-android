package io.scalac.degree.android.view.listholder;

import android.support.v7.widget.RecyclerView;
import android.view.View;

public class BaseScheduleDayLineupHolder extends RecyclerView.ViewHolder {
    public BaseScheduleDayLineupHolder(View itemView) {
        super(itemView);
    }
}
